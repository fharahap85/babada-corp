name: No Deploy

on:
  push:
    branches:
      - develop

  workflow_dispatch:

jobs:
  noop:
    name: Git only — deploy via cPanel
    runs-on: ubuntu-latest
    steps:
      - run: echo "Staging is deployed manually via cPanel File Manager"